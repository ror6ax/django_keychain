language: python
python:
  - "2.7"
  - "3.6"

env:
  - DJANGO_VERSION=2.1.2

install:
  - pip install -r requirements.txt

script:
  - python runtests.py
  
deploy:
  provider: pypi
  user: ror6ax
  password:
    secure: "Your password"
